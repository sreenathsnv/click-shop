import { AvgRatingPipe } from './avg-rating.pipe';

describe('AvgRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new AvgRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
