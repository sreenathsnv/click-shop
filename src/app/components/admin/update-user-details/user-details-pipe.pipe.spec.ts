import { UserDetailsPipePipe } from './user-details-pipe.pipe';

describe('UserDetailsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UserDetailsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
